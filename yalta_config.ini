# Yalta runtime configuration
# Bool values: true/false, yes/no, on/off, 1/0

# AI-controlled players
ai.red=true
ai.white=true
ai.black=true

# Search depth (clamped to [1..6])
ai.depth=2

# Optional: parallel search at root
ai.parallel_root=true